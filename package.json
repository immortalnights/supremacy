{
  "name": "sup-mp-react",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev-client": "cd client && npm start",
    "build-sim": "cd simulation && tsc --watch",
    "dev": "concurrently --prefix \"[{name}]\" --names \"   SIM,CLIENT\" -c \"bgBlue.bold,bgGreen.bold\" \"npm run build-sim\" \"npm run dev-client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0",
    "typescript": "^4.5.5"
  }
}
